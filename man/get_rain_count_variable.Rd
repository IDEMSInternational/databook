% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epicsa_functions.R
\name{get_rain_count_variable}
\alias{get_rain_count_variable}
\title{Extract rain-day count rule (sign and threshold)}
\usage{
get_rain_count_variable(
  daily_data_calcs,
  rainfall_column = "extreme_rain",
  list_name = NULL
)
}
\arguments{
\item{daily_data_calcs}{A list-like object of daily data calculations
passed to \code{get_r_instat_definitions()}.}

\item{rainfall_column}{Character. Name of the rain-related element in
\code{daily_data_calcs} to inspect (default \code{"extreme_rain"}).}

\item{list_name}{Optional name; if given, the result is wrapped and named
with this value.}
}
\value{
A list with elements \code{sign} and \code{threshold}.
If \code{list_name} is supplied, returns a named list of that list.
}
\description{
Parses the rain-day definition embedded in a daily-data calculation list
(from R-Instat style objects) and returns the comparison sign and threshold
used to count a "rain day" (e.g., \code{">="} and \code{"1"}).
}
\examples{
# res <- get_rain_count_variable(daily_data_calcs, "rain_days")
# res$sign      # e.g. ">="
# res$threshold # e.g. "1"
}
\seealso{
get_r_instat_definitions()
}
